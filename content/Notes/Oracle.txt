Q : date function
A : to_char(sysdate,'YYYY/MM/DD HH24:MI:SS')

Q : cast function
A : to_char, to_number

Q : top n records
A : select * from (select * from table_name) where rownum < n + 1;

Q : add time, minus time
A : sysdate +- (60/60*24)

Q : execute store procedure
A :
begin
  P_INIT_USER_RET_POINT_77536_77541();
  commit;
end;

Q : show all sequence
A :
select sequence_owner, sequence_name from dba_sequences;


DBA_SEQUENCES -- all sequences that exist
ALL_SEQUENCES  -- all sequences that you have permission to see
USER_SEQUENCES  -- all sequences that you own

Q : use seq
A : insert into table(id, <columns>) values (sequence.nextval, <values>);